---
title: Somerset Railwayana
---

style.
  | @keyframes fadeBetweenImages {
  |   0% {
  |     opacity: 1;
  |     background-image: url(#{image_path(data.pages.home.hero.images[0].image)});
  |   }
  |   25% {
  |     opacity: 1;
  |     background-color: white;
  |   }
  |   50% {
  |     opacity: 1;
  |     background-image: url(#{image_path(data.pages.home.hero.images[1].image)});
  |   }
  |   100% {
  |     opacity: 1;
  |     background-color: white;
  |   }
  | }
  |
  | section.hero {
  |   animation: fadeBetweenImages 12s linear infinite;
  |   background-repeat: no-repeat;
  |   background-size: cover;
  | }

- data.pages.home.hero.images.each do |image|
  div.preload-image style="background-image: url(#{image_path(image.image)}); display: none;"

section.hero.centered-container
  .caption.centered-text
    .row
      h1 = p = data.pages.home.hero.title

section.homepage-story.centered-container
  .container
    .row
      .story-content.centered-text
        = markdown data.pages.home.story

section.homepage-products.products.centered-container
  .container
    h2.centered-text
      | Current Auction Items
    .row.centered-row
    - data.products.sort {|(_filename_a, a),(_filename_b, b)| b.date <=> a.date}.first(3).each do | _filename, product |
      = partial 'partials/products/product', locals: {product: product}

section.homepage-news.news.centered-container
  .container
    h2.centered-text
      | Previous Auctions
    .news-items.centered-row
    - blog.articles.first(2).each do | article |
      = partial 'partials/news/news_item', locals: {article: article}
